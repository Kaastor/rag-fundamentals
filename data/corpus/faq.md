# FAQ

**Q: Does Frobnicator work offline?**  
A: Yes, core features work offline; cloud features require a key.

**Q: Where is the configuration file stored?**  
A: In your home directory at `~/.frob/config.yaml`.

**Q: What is the default for `level`?**  
A: The default `level` is 3.
